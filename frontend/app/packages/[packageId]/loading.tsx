export default function PackageLoading() {
  return (
    <main className="state-shell">
      <section className="hero-card state-card">
        <span className="state-eyebrow">Loading Packet</span>
        <h1 className="state-title">Opening packet review workspace</h1>
        <p className="state-body">
          Loading document metadata, findings, extracted fields, and reviewer
          history for this package.
        </p>
      </section>
    </main>
  );
}
