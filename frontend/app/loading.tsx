export default function Loading() {
  return (
    <main className="state-shell">
      <section className="hero-card state-card">
        <span className="state-eyebrow">Loading</span>
        <h1 className="state-title">Preparing the review workspace</h1>
        <p className="state-body">
          We&apos;re loading packet data, findings, and evidence so the reviewer
          workspace can render with the latest state.
        </p>
      </section>
    </main>
  );
}
